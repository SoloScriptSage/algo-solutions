#include <bits/stdc++.h>

#define endl '\n'

typedef long long ll;
typedef long double ld;
using namespace std;

string s[52];


string alphabet = "abcdefghijklmnopqrstuvwxyz"
ll n, sum1, sum2, num, mini = 10000008, t;
ll a[10000008];
ll x[108], y[108];
ld d[108][108];


int main()
{

    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> t;

    for(int i = 0; i < t; ++i)
    {
        cin >> n >> amount;
        for(int j = 0; j < n; j++)
        {
            cin >> s[j];
        }

        for(int j = 0; j < amount; j++)
        {
            if(s1[j] != s2[j])
            {
                int ind_1 = alphabet.find(s1[j]);
                int ind_2 = alphabet.find(s2[j]);

                if(ind_1 > )
            }
        }
    }

    return 0;
}


